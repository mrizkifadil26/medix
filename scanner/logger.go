package scanner

/*
func getLogLevel(opts ScanOptions) int {
	if opts.Trace {
		return LevelTrace
	}

	if opts.Debug {
		return LevelDebug
	}

	if opts.Verbose {
		return LevelInfo
	}

	return LevelError
}
*/
